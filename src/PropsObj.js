function PropsObj(props)
{

    return(

<p>{props.cars.brand} is my fav car and {props.cars.model} model i like the most, especially with {props.cars.color} finish. </p>
    );
}
export default PropsObj;